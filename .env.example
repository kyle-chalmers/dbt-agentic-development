# dbt Agentic Development — Environment Variables
# Copy to .env and fill in your values

# Optional: Custom profiles.yml location (default: ~/.dbt/)
# DBT_PROFILES_DIR=./

# Note: dbt Agent Skills and dbt MCP Server don't require env vars.
# DuckDB adapter runs locally with zero database setup.
